package util;

public class Kinder extends Agedp{

   public Kinder(int x){
       /*se tiene que hacer referencia al contructor de la clase padre con super 
       * por ende se debe crear un constructor vacio en la clase padre
       * */

       super();
   }
    
   // Debe llevar un metodo main para que el programa compile
    public static void main(String[] args) {
        
    }
}
